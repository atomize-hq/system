interface:
  display_name: "handbook"
  short_description: "Thin discovery metadata for the handbook family and the handbook-charter-intake workflow."
  default_prompt: "Use handbook for this task."
policy:
  allow_implicit_invocation: true
