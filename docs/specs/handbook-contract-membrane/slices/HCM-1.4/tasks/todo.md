# Tasks: HCM-1.4 Profile-Aware Setup and Doctor Decisions

All implementation checkboxes remain unchecked in this planning session.

## Entry and authority

- [ ] Verify exact reviewed HCM-1.4 planning commit, fingerprint, handoff, and ledger parity.
- [ ] Confirm clean `feat/handbook-contract-membrane` worktree and HCM-1.1/HCM-1.2/HCM-1.3 ancestry.
- [ ] Re-read all required skills and exact authority sections.
- [ ] Run `npx gitnexus analyze --index-only` and fresh upstream impact for every existing symbol to edit.
- [ ] Warn before editing HIGH-risk `run_setup` and record its live blast radius.
- [ ] Reconfirm exact allowed paths, non-goals, proof wall, and stop conditions.

## Packet 1 — Condition and profile decisions

- [ ] RED: exact public type/accessor compilation tests fail.
- [ ] RED: exact always/optional/conditional-unresolved applicability, missing-definition refusal, ordering, and input-surface-absence tests fail.
- [ ] GREEN: retain/expose the selected condition registry without changing profile identity.
- [ ] GREEN: bind exact conditional definitions and emit only `unresolved`/`evidence_contract_unavailable`/`indeterminate` with null evidence fingerprint.
- [ ] GREEN: implement the exact explicit package-owned shipped-profile decision resolver with no ambient/default inference.
- [ ] GREEN: implement one immutable `ResolvedProfileDecisions` closure and exact ordered accessors.
- [ ] Verify HCM-1.2/HCM-1.3 source, profile, registry, and package tests remain green.

## Packet 2 — Repository inspection

- [ ] RED: generated shipped/custom repository inspection matrix fails.
- [ ] RED: size, duplicate YAML, wrong schema, symlink, race, unreadable, and permutation cases fail.
- [ ] RED: one named test for every frozen low-level outcome to exact status/reason pair fails.
- [ ] GREEN: implement selected-path-only bounded no-follow reads.
- [ ] GREEN: parse YAML into the JSON data model and validate by instance registry routing.
- [ ] GREEN: return complete ordered typed inspection results with no authority overclaim.
- [ ] Verify no definition asset, package manifest, or Cargo byte changed.

## Packet 3 — Setup cutover

- [ ] RED: exact compiler setup types/signatures/rows/errors, SetupMode wire values, total request/root/error-payload map, SetupError code/ALL/accessors, and profile-aware action/status tests fail because the APIs are absent.
- [ ] GREEN/boundary: all 13 codes exhaustively project to the frozen rows; independent `compile_fail,E0451` struct-literal and `compile_fail,E0624` constructor doctests pass after the API exists.
- [ ] RED: no-write, rewrite-refusal, reset-preflight, mode, custom-instance, and status-precedence tests fail.
- [ ] GREEN: replace fixed artifact planning with the shared profile closure.
- [ ] GREEN: remove selected-artifact starter writes and return `author_required`.
- [ ] GREEN: refuse rewrite before mutation; run unchanged reset only after complete valid/non-indeterminate profile preflight.
- [ ] GREEN: fail closed on non-directory/symlink roots; create only a missing root; retain no invalid-root repair branch.
- [ ] Report legacy reset apply failure honestly without changing `route_state.rs` or claiming rollback.
- [ ] Remove only obsolete setup fixed-artifact imports/helpers.
- [ ] Verify setup source contains no fixed artifact decision/compatibility dispatch.
- [ ] Replace only compiler author test `scaffold_repo`/imports with the exact test-local legacy fixture helper; run all 47 author tests.

## Packet 4 — Doctor and CLI cutover

- [ ] RED: exact closed doctor report, two-variant error mapping, and status-precedence tests fail.
- [ ] GREEN: make doctor consume the same decision/inspection closure as setup.
- [ ] GREEN: exhaustively replay `DoctorError::ALL` against the exact two-row derived kind/reason mapping.
- [ ] Prove setup/doctor profile, condition, applicability, path, role, and four-field capability-identity equality.
- [ ] RED: CLI text/JSON/status/exit tests fail against the new typed report.
- [ ] GREEN: render human wording only in CLI and emit exact JSON with one trailing LF.
- [ ] Preserve command grammar and root discovery; add no profile/condition input flag or generated command.
- [ ] Remove only obsolete doctor/setup wording helpers from compiler.
- [ ] Replace only CLI author test `scaffold_repo`/imports with the exact test-local legacy fixture helper; run all 22 author tests.

## Security and boundaries

- [ ] Prove no environment/cwd/home/PATH/filename/profile-flag inference.
- [ ] Prove no observation/evidence/freshness/assertion/bare-boolean/override input surface and no outcome other than explicit unresolved.
- [ ] Prove file/environment/profile/CLI state cannot change unresolved or populate an evidence fingerprint.
- [ ] Prove capability rows contain exactly the four identity fields and make no binding/cardinality/validator/rule closure claim.
- [ ] Prove no absolute/control/unbounded error disclosure.
- [ ] Prove no intermediate/final symlink or substitution-race escape.
- [ ] Prove no raw artifact bytes, credentials, or environment values in results/errors.
- [ ] Prove no canonical artifact write, deletion, rewrite, or condition-driven scaffold.
- [ ] Prove mutation starts only after complete safe deterministic preflight.

## Determinism and regressions

- [ ] Prove profile-source/filesystem/map permutations are identical.
- [ ] Run focused HCM-1.4 decision and inspection suites.
- [ ] Run HCM-1.2 and HCM-1.3 profile/registry regression suites.
- [ ] Run compiler setup/doctor and complete compiler suite.
- [ ] Run CLI surface and complete workspace suites.
- [ ] Run format, clippy, and Windows cross-compilation checks.
- [ ] Append only `crates/engine/definitions/** text eol=lf` to `.gitattributes`; prove all 29 index blobs/fingerprints are unchanged and every member resolves to `text: set`, `eol: lf`.
- [ ] In a fresh native-Windows clone with `core.autocrlf=true`, prove all 29 checked-out definition members are byte-equal to their index blobs.
- [ ] Execute the named inspection/setup refusal tests on an actual Windows MSVC host; stop if unavailable.
- [ ] Inventory every retired setup/doctor test and its named profile-aware replacement.
- [ ] Keep author assertions unchanged except the two exact fixture helpers; keep manifest/freshness/flow/rendering tests unchanged and green.
- [ ] RED: reproduce the compiler Windows MSVC `libc::LOCK_EX` failure before the portability edit.
- [ ] Re-run HIGH-risk `acquire_authoring_lock` impact and warn before editing.
- [ ] GREEN: cfg-select only the local `author/mod.rs` acquisition operation as Unix `LOCK_EX` or non-Unix ignored `0` before the unchanged helper call.
- [ ] Prove helper signatures/bodies and Unix `Drop` `LOCK_UN` stay byte-unchanged; prove Unix acquire/retry/unlock and non-Unix existing no-op behavior; rerun compiler MSVC check plus all 47/22 author tests.

## Package and scope proof

- [ ] Replay the literal 29-member definition manifest against tree and engine package by set, bytes, size, and SHA-256.
- [ ] Package/extract/check engine with no fixture or absolute-path leak.
- [ ] Prove compiler source-tree/workspace metadata, unchanged Cargo manifest, tests, exact changed paths, and no fixture/absolute-path leak; claim no compiler package.
- [ ] Prove exact allowed changed-path set equality.
- [ ] Run fixed-symbol/import absence and forbidden-scope scans.
- [ ] Run secret-pattern and untracked-aware whitespace scans.
- [ ] Run all three handoff validator modes.
- [ ] Run `git diff --check` and scoped diff inspection.

## Control pack, review, and closeout

- [ ] Update only affected `00`, `03`, `04`, and `06` truth with the exact evidence ceiling.
- [ ] Write immutable HCM-1.4 implementation proof wall.
- [ ] Create schema-valid immutable review dispatch with exact manifest/fingerprint and raw proof refs.
- [ ] Spawn fresh isolated read-only built-in `default` reviewer and collect structured findings.
- [ ] Validate/remediate every actionable finding; rerun full proof; use a different fresh reviewer.
- [ ] Obtain final `CLEAN` over the complete exact subject.
- [ ] Replay final clean manifest byte-for-byte and run staged GitNexus/diff/scope/secret gates.
- [ ] Commit reviewed implementation/control-pack/proof subject.
- [ ] Create one parent v1.2 completed handoff; rebuild/validate ledger in all modes.
- [ ] Commit only mechanical handoff/ledger closeout artifacts.
- [ ] Report both hashes and stop without starting HCM-2.

## Permanent stop checks

- [ ] Stop if broader authority or a file outside the allowlist, including any repository-root change other than the exact admitted definition-tree LF `.gitattributes` rule, is required.
- [ ] Stop if a fixed-universe bridge, legacy profile, fallback, or dual path is required.
- [ ] Stop if canonical content write/authority/intake/renderer/Projection work is required.
- [ ] Stop if Cargo, definition assets, schemas, or new CLI input grammar is required.
- [ ] Stop if a condition evidence/evaluator contract, outcome other than unresolved, transactional reset repair, or compiler publication is required.
- [ ] Stop if HIGH/CRITICAL impact cannot be contained or platform proof is unavailable.
- [ ] Stop if the final independent review cannot be completed cleanly.
