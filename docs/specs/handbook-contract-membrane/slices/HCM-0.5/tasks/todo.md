# HCM-0.5 Forward Task Ledger

Only future work for the approved fresh design-freeze session belongs here. Generic orchestration, review, remediation, true-stop, handoff, ledger, and closeout mechanics remain owned by canonical [`07`](../../../07-orchestration-onboarding-prompt.md) and [`08`](../../../08-handoff-ledger-and-escalation-protocol.md).

## Execution-entry gate

```text
review-clean packet commit -> validated v1.2 context-boundary handoff binding that commit, this packet path, and the reviewed packet fingerprint -> fresh design-freeze session selecting that exact handoff -> live repository and packet-identity validation by that fresh session -> canonical HCM-0.5 design edits may begin
```

- [ ] Use the exact approved selector supplied by the review-clean packet closeout; do not substitute an existing non-clean HCM-0.5 handoff.

## Entry validation

- [ ] Validate the selected v1.2 handoff, ledger parity, review-clean packet commit, exact packet path, and recomputable packet fingerprint.
- [ ] Validate live branch, HEAD, status, packet bytes, canonical `05` identity, and absence of unsafe overlapping work.
- [ ] Confirm HCM-0.4 as completed dependency evidence only and preserve its frozen owner/operation/DTO/transport/bridge/publication semantics.
- [ ] Confirm HCM-0.9 is abandoned evidence only and supplies no resume, topology, or catalog-leaf authority.
- [ ] Confirm documentation/design-only scope and keep all runtime proof gates open.

## Canonical `00`-`06` design freeze

- [ ] Author the final intended `00-README.md` HCM-0.5 status bytes before proof and final review.
- [ ] Tighten `01-target-architecture.md` owner, validator-witness, process-dock isolation, and future-Rust semantic-parity boundaries.
- [ ] Add contract/evidence/applicability/Resolution semantics to `02-semantic-model.md` without altering HCM-0.3 authority.
- [ ] Update `03-seam-crosswalk.md` with frozen target semantics, the selected first proof dock, and still-open runtime classifications.
- [ ] Expand the HCM-0.5 dependency/output/exit/non-goal contract in `04-phase-slice-map.md` without starting HCM-0.6.
- [ ] Replace only preliminary HCM-0.5 sections in canonical `05-contracts-schemas-and-gates.md`; keep the monolith and no-leaf boundary.
- [ ] Freeze contract identity/fingerprint/version compatibility/lifecycle and keep evaluation state separate.
- [ ] Freeze claims/applicability and evidence identity/provenance/freshness/cardinality/precedence/Resolution limits.
- [ ] Freeze the closed verdict vocabulary, claim partition, gate composition, weighted-score limits, and hard-fail behavior.
- [ ] Freeze dock capability, exact implementation-bundle/runtime-closure identity, process JSON request/result, grants/isolation/no-network, timeout/cancellation/refusal/crash/cleanup, and validator-as-witness semantics.
- [ ] Select `handbook.dock.json-schema@1.0.0` as the bounded local Draft 2020-12 first proof target without claiming implementation or runtime proof.
- [ ] Append only the approved HCM-0.4-compatible ordinary contract/dock operation definitions.
- [ ] Add HCM-0.5 proof/regression authority to `06-proof-and-regression-ledger.md` while keeping `PG-CONTRACT-01`, `PG-DOCK-01`, and `PG-GATE-01` open.
- [ ] Leave Rust, Cargo, CLI, SDK, Tauri, Substrate, runtime, validators, schemas, HCM-0.6, HCM-0.9, and catalog leaves unchanged.

## Complete proof and regression wall

- [ ] Parse all new JSON/YAML fenced examples with required duplicate-key rejection.
- [ ] Assert contract identity/SemVer, immutable lifecycle adjacency/authority, applicability, evidence partition/cardinality/precedence/freshness/Resolution, verdict/gate, and hard-fail rules.
- [ ] Assert manifest/implementation/runtime-closure identity, process framing, capability intersection, default-deny isolation, unconditional v1 network denial, timeout/cancellation/failure, and no-partial-evidence rules.
- [ ] Exercise every required negative/refusal case, including stale, substituted, malformed, missing, conflicting, under-Resolution, refused, timed-out, cancelled, crashed, oversized, and cleanup-uncertain outcomes.
- [ ] Assert the selected JSON Schema dock remains bounded and the HCM-0.5 ordinary operation additions conform to frozen HCM-0.4 semantics.
- [ ] Regression-check HCM-0.2/0.3/0.4 frozen authority, canonical monolith/no-leaf rules, open runtime gates, and no-HCM-0.6 boundary.
- [ ] Validate Markdown links, anchors, fences, archive boundary, portable references, handoff validators, `git diff --check`, scoped diff, docs-only paths, and repository-required change detection.
- [ ] Preserve the complete proof results before final canonical review.

## Fresh independent review

- [ ] Bind every intended canonical byte, all three packet files, complete proof results, and final `00-README.md` status bytes into the final review manifest.
- [ ] Obtain fresh independent review through canonical `07`/`08`.
- [ ] Validate findings against live packet authority; remediate only accepted in-scope blockers.
- [ ] After any accepted remediation, rerun the complete proof wall and obtain different-fresh re-review.
- [ ] Require final `CLEAN` over the exact complete subject; never complete from a non-clean subject.
- [ ] Do not mutate any subject byte after `CLEAN`.

## Byte-identical commit and closeout

- [ ] Replay the complete proof wall against bytes identical to the clean manifest.
- [ ] Stage only the clean subject and authorized proof/review evidence; verify the staged fingerprint and exact `00-README.md` hash against the clean manifest.
- [ ] Run staged `git diff --check`, scoped inspection, and repository-required change detection.
- [ ] Commit the exact reviewed HCM-0.5 subject first.
- [ ] Create the parent-owned completed v1.2 handoff bound to the primary commit, clean fingerprint, proof, and review/remediation lineage.
- [ ] Rebuild and validate the ledger and all canonical `08` validator modes.
- [ ] Commit only the mechanical handoff/ledger closeout artifacts in a separate commit.
- [ ] If completion is impossible, use canonical orchestration closeout rather than adding a feature task or mutating a non-clean subject.
- [ ] Do not auto-start HCM-0.6.
