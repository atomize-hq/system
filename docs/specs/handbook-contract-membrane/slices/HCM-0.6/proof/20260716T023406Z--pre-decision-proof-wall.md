# HCM-0.6 Pre-Decision Proof Wall

## Boundary and result

- Phase/slice: `HCM-0` / `HCM-0.6`
- Parent orchestration: `20260716T021902Z--HCM-0-6--orchestration`
- Baseline HEAD: `c0cc03b82a1fcc512d34cd4797f4fe46a819acc5`
- Result: research-complete and decision-ready; user approval is still absent
- Gate posture: `PG-DEFAULT-01` remains open
- Maximum promotion: reviewed pre-decision research subject only; no shipped
  kind, instance, path, label, condition, or requiredness is approved

## Dependency and authority proof

- Live HCM-0.2, HCM-0.3, HCM-0.4, HCM-0.5, and HCM-0.8 completion records
  were re-read as dependency evidence; no handoff selected or reopened them.
- The HCM-0.6 phase-map row, frozen kind/instance/requiredness and
  constitutional-root semantics, `PG-DEFAULT-01`, and the current
  orchestration protocol were used as authority.
- Current implementation/examples were admitted only as precedent and cost
  evidence.
- The worktree was clean at entry and all current changes are parent-owned
  HCM-0.6 slice/dispatch/proof artifacts.

## Built-in delegated research proof

| Dispatch | Built-in agent | Type/freshness | Final status | Verdict | Subject |
|---|---|---|---|---|---|
| `docs/specs/handbook-contract-membrane/handoffs/dispatches/20260716T021902Z--HCM-0-6--repository-truth-research.json` | `/root/hcm_0_6_repo_research` | `default`; isolated fresh context | `completed` / read-only | `COMPLETE` | `sha256:db27d0c16f96c5935fa548acb1c0bb8c40ae67a8f3e354e6ff42a5e867de2225` |
| `docs/specs/handbook-contract-membrane/handoffs/dispatches/20260716T021903Z--HCM-0-6--external-primary-source-research.json` | `/root/hcm_0_6_external_research` | `default`; isolated fresh context | `completed` / read-only | `COMPLETE` | `sha256:8f3acb3a9a00ca4ad8bd14f3e82fa1c22a1fbfd96cf1ca1f7e9b43a6ed7e4eb2` |

Both agents replayed every manifest entry and aggregate before research, returned
through the built-in channel, remained read-only, and wrote no global handoff or
ledger entry. The parent accepted both as decision-preparation evidence and
reconciled their sourced facts while retaining approval authority with the user.

## Research coverage

- Repository evidence covers the fixed four-kind universe, descriptors,
  requiredness ambiguity, setup/doctor/flow coupling, structured authoring,
  Feature Spec project/work path split, Environment/Foundation duplication,
  other declarative planning/foundation families, and Markdown/YAML cutover
  cost.
- External research covers constitutional governance, architecture context,
  decisions, work/change specifications, environment/configuration truth,
  risk/quality, operational/runbook and incident learning, progressive
  documentation, and tiered/versioned rigor.
- The dossier contains 21 unique primary-source HTTPS links. All returned HTTP
  200 during parent reachability verification on 2026-07-16.
- Minimal, Standard, and Full candidates use the same rubric and separately list
  shipped kinds, selected default instances, requiredness/conditions,
  scope/cardinality, lifecycle/review triggers, renderer/Projection posture, and
  compatibility with the frozen SDK/transport owner split.
- Candidate B is labeled as a parent recommendation/hypothesis, never approval.

## Review 1 findings and parent remediation

Fresh reviewer `/root/hcm_0_6_predecision_review_1` replayed exact subject
`sha256:74c8172b1db001b9c8374f700eaaa57bd7a8e9d37c19e84a4b6df2108c9e00a8`
from immutable dispatch
`20260716T023810Z--HCM-0-6--fresh-pre-decision-review-1` and returned
`CHANGES_REQUIRED` with two Required findings and no Critical finding.

The parent accepted and remediated both findings:

1. the comparison now maps every frozen SPEC rubric axis into the shared table
   and gives each candidate consistent scope/cardinality, lifecycle/review,
   fixed-renderer/future-Projection, and SDK/transport posture; and
2. the proof wall now includes an untracked-aware byte scan over the exact six
   added Markdown files instead of presenting ordinary `git diff --check` as
   coverage of untracked bytes.

A different fresh reviewer must assess the regenerated complete subject.

## Raw verification results

| Command/check | Result |
|---|---|
| Exact untracked-aware six-file Markdown byte scan | exit 0; all six added Markdown files checked for CRLF, trailing spaces/tabs, NUL bytes, UTF-8 decoding, and terminal newline |
| `git diff --check` | exit 0; tracked-diff check only, not claimed as coverage of the untracked packet |
| `python3 tools/check_archive_boundary.py` | exit 0 |
| `python3 tools/check_archive_boundary.py --self-test` | exit 0; forbidden fixture rejected |
| `python3 docs/specs/handbook-contract-membrane/handoffs/validate_handoffs.py` | exit 0; 31 records, 90 current internal dispatches, 8 admitted legacy dispatches, exact ledger |
| `validate_handoffs.py --self-test-v1-admission` | exit 0; unknown/modified/deleted historical records and dispatches rejected |
| `validate_handoffs.py --self-test-orchestration-contract` | exit 0; delegation, remediation/re-review, stop/status, fingerprint, and two-commit failures rejected |
| Parent HCM-0.6 semantic assertion harness | exit 0; 18 authority/candidate/gate assertions passed |
| Parent source-link extraction/reachability harness | exit 0; 21 unique primary-source HTTPS URLs returned HTTP 200 |

## GitNexus posture

- The stale Handbook graph was refreshed incrementally from 13 commits behind
  through current HEAD before research.
- The installed CLI did not support the graph service's suggested
  `--repair-fts`; a forced rebuild terminated with the known N-API failure.
- No keyword-graph claim was invented. Exact live sources and available symbol
  context were used for inventory.
- No code symbol was edited, so no symbol impact analysis was applicable.
- Repository-required change detection remains a final pre-commit gate.

## Negative and scope proof

- No Rust, Cargo, runtime, schema, CLI, SDK, Tauri, Substrate, dock, or HCM-0.7
  file changed.
- No current enum/template/filename is described as approved default authority.
- No candidate selects more or fewer than one always-required
  `constitutional_root` instance.
- Work Specification and Decision Record are modeled as on-demand
  multi-instance kinds rather than permanent project singletons.
- Environment, risk, runbook, quality, and catalog roles expose applicability,
  overlap, drift, and onboarding tradeoffs instead of defaulting by inertia.
- The candidate comparison explicitly says that shipped kinds and selected
  default instances are different axes.
- The user-decision record does not exist and `PG-DEFAULT-01` is not promoted.

## Remaining gate

A fresh independent reviewer must replay the complete pre-decision subject and
report no valid Critical or Required finding. After a clean review, the parent
may commit this decision-ready state and stop with `human_input`. HCM-0.6 itself
remains open until the user makes the explicit decision and a later final review
and proof wall close the approved subject.
