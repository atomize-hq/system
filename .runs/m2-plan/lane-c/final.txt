Current blocker: M2 Lane C is blocked by the committed fixture shape.

[core/stages/10_feature_spec.md](/Users/spensermcconnell/__Active_Code/system/core/stages/10_feature_spec.md) exists, but neither the real pipeline [pipelines/foundation_inputs.yaml](/Users/spensermcconnell/__Active_Code/system/pipelines/foundation_inputs.yaml) nor the shared proof-corpus pipeline [foundation_inputs.yaml](/Users/spensermcconnell/__Active_Code/system/tests/fixtures/pipeline_proof_corpus/foundation_inputs/repo/pipelines/foundation_inputs.yaml) declares `stage.10_feature_spec`. The shared corpus repo also does not contain the compile-time inputs that stage needs: no fixture copy of `core/rules/*.md`, no `profiles/python-uv/commands.yaml`, no `profiles/python-uv/conventions.md`, no `core/library/feature_spec/*`, and no `artifacts/...` inputs for base/charter/foundation. M2 cannot be called shipped until that is fixed.

**Draft**
### Lane C. Proof Corpus, Compile Goldens, and Help/Docs Conformance

- Keep M2 proof narrow and explicit: one shared foundation-family proof corpus, one supported compile target, `pipeline.foundation_inputs` + `stage.10_feature_spec`, and no second corpus tree for CLI vs compiler.
- Extend the real pipeline at [pipelines/foundation_inputs.yaml](/Users/spensermcconnell/__Active_Code/system/pipelines/foundation_inputs.yaml) and the shared proof-corpus pipeline at [tests/fixtures/pipeline_proof_corpus/foundation_inputs/repo/pipelines/foundation_inputs.yaml](/Users/spensermcconnell/__Active_Code/system/tests/fixtures/pipeline_proof_corpus/foundation_inputs/repo/pipelines/foundation_inputs.yaml) to append `stage.10_feature_spec` after `stage.07_foundation_pack`.
- Add the stage file to the shared proof corpus at [tests/fixtures/pipeline_proof_corpus/foundation_inputs/repo/core/stages/10_feature_spec.md](/Users/spensermcconnell/__Active_Code/system/tests/fixtures/pipeline_proof_corpus/foundation_inputs/repo/core/stages/10_feature_spec.md), matching the current stage contract from [core/stages/10_feature_spec.md](/Users/spensermcconnell/__Active_Code/system/core/stages/10_feature_spec.md).
- Add every compile-time dependency referenced by `stage.10_feature_spec` into the shared corpus repo, with realistic committed content, not placeholders:
  - `core/rules/p0_absolute.md`
  - `core/rules/p1_pragmatic.md`
  - `core/rules/traceability_policy.md`
  - `core/rules/evidence_policy.md`
  - `profiles/python-uv/profile.yaml`
  - `profiles/python-uv/commands.yaml`
  - `profiles/python-uv/conventions.md`
  - `runners/codex-cli.md`
  - `core/library/feature_spec/feature_spec_architect_directive.md`
  - `core/library/feature_spec/FEATURE_SPEC.md.tmpl`
- Add realistic upstream artifact inputs under the shared corpus repo so compile can resolve stage.10 inputs without synthesizing fake runtime output:
  - required: `artifacts/base/BASE_CONTEXT.md`, `artifacts/charter/CHARTER.md`
  - optional happy-path baseline: `artifacts/project_context/PROJECT_CONTEXT.md` plus the full foundation pack files from [core/stages/07_foundation_pack.md](/Users/spensermcconnell/__Active_Code/system/core/stages/07_foundation_pack.md)
- Keep optional-input coverage non-duplicative: the committed corpus baseline should be the richer “full context” case, and the optional-absence success case should delete optional files from the temp installed copy during tests instead of creating a second near-identical fixture tree.
- Add route-basis fixture support to the shared corpus. Fresh route-basis snapshots should be created by `pipeline resolve` inside tests so the real resolve→compile handoff is exercised. Only malformed-route-basis refusal should use a committed negative seed. Do not add a second hand-authored “happy-path route_basis” source of truth.
- Extend proof normalization helpers so compile goldens can replace temp repo paths and the persisted route-basis path with stable placeholders; the current helpers only cover `{{REPO_ROOT}}` and `{{STATE_PATH}}`.

- Lock one shared success payload golden and one shared success explain golden for the baseline full-context corpus:
  - `compile.stage_10_feature_spec.payload.full_context.txt`
  - `compile.stage_10_feature_spec.explain.full_context.txt`
- The payload golden must pin the plain `pipeline compile` stdout shape only.
- The explain golden must pin `pipeline compile --explain` as proof-only output for: selected pipeline/stage, route-basis summary, include expansion, required vs optional input decisions, and outputs contract.
- Compiler and CLI tests must read those same golden files from [tests/fixtures/pipeline_proof_corpus/foundation_inputs/goldens](/Users/spensermcconnell/__Active_Code/system/tests/fixtures/pipeline_proof_corpus/foundation_inputs/goldens); no duplicate compiler-only or cli-only compile goldens.
- Stale-route-basis and inactive-stage refusals are regression-critical. Compiler tests may type-assert refusal kinds, but CLI tests must pin operator-facing wording and the “re-run `pipeline resolve`” recovery path.

- Compiler responsibilities:
  - add a dedicated compile-core suite, preferably separate from the existing route-only files, to assert typed compile result assembly, required/optional input classification, shared render-from-one-result behavior, and refusal classification
  - exercise the shared corpus by installing the committed fixture repo, running resolve when a fresh basis is needed, and comparing compiler-rendered payload/proof output against the shared goldens
- CLI responsibilities:
  - add compile surface integration coverage for selector parsing, exit codes, stdout/refusal rendering, and the resolve→compile handoff
  - keep compile help and docs drift checks in the CLI suite, not in compiler tests
- Shared-fixture rule:
  - the source of truth stays under [tests/fixtures/pipeline_proof_corpus/foundation_inputs](/Users/spensermcconnell/__Active_Code/system/tests/fixtures/pipeline_proof_corpus/foundation_inputs)
  - helper code may exist in both suites if Cargo wiring forces it, but fixture contents, seeds, and goldens must exist only once

- Mandatory M2 regression tests for Lane C:
  - missing persisted `route_basis` refusal
  - malformed persisted `route_basis` refusal
  - stale `route_basis` refusal after route-state mutation without a fresh resolve
  - inactive-stage refusal when `stage.10_feature_spec` is present in the pipeline but not active in the persisted route basis
  - selected-stage-not-in-pipeline refusal
  - missing required artifact refusal for `stage.10_feature_spec`
  - optional-artifact absence success for `stage.10_feature_spec`
  - plain `pipeline compile` payload-only success
  - `pipeline compile --explain` proof-only success
  - one shared typed compile result proving payload/explain cannot drift semantically

- The stale-route-basis regression should be isolated by:
  - resolving a fresh basis for an active `stage.10_feature_spec`
  - mutating `run.profile` or `run.runner`
  - compiling without a fresh resolve
  - asserting stale-basis refusal, not inactive-stage refusal
- The inactive-stage regression should be isolated by:
  - resolving from a route state where `stage.06_project_context_interview` is still `next`
  - compiling `stage.10_feature_spec`
  - asserting inactive-stage refusal that surfaces the blocking route condition, not a freshness refusal

- Help/doc drift coverage that must change when compile lands:
  - update [system-help.txt](/Users/spensermcconnell/__Active_Code/system/crates/cli/tests/snapshots/system-help.txt) so top-level help still presents setup-first ordering and now advertises `pipeline compile`
  - update [system-pipeline-help.txt](/Users/spensermcconnell/__Active_Code/system/crates/cli/tests/snapshots/system-pipeline-help.txt) so `compile` appears beside `list`, `show`, `resolve`, and `state`
  - add `system-pipeline-compile-help.txt`
  - update [help_drift_guard.rs](/Users/spensermcconnell/__Active_Code/system/crates/cli/tests/help_drift_guard.rs) so docs/help parity now requires `pipeline compile`, `pipeline compile --explain`, payload-only stdout wording, and the freshness recovery path
  - update support-story docs checked by that drift guard: [README.md](/Users/spensermcconnell/__Active_Code/system/README.md), [docs/START_HERE.md](/Users/spensermcconnell/__Active_Code/system/docs/START_HERE.md), [docs/SUPPORTED_COMMANDS.md](/Users/spensermcconnell/__Active_Code/system/docs/SUPPORTED_COMMANDS.md), and keep them aligned with [C-02](/Users/spensermcconnell/__Active_Code/system/docs/contracts/C-02-rust-workspace-and-cli-command-surface.md)
  - remove the current “compile remains deferred” wording from [docs/SUPPORTED_COMMANDS.md](/Users/spensermcconnell/__Active_Code/system/docs/SUPPORTED_COMMANDS.md) once the command actually ships

- Implementation sequencing for Lane C:
  1. Update the real and proof-corpus `foundation_inputs` pipelines to declare `stage.10_feature_spec`.
  2. Expand the shared proof corpus with the stage file, compile includes, library files, and realistic artifact inputs.
  3. Add route-basis fixture support and path normalization needed for compile goldens.
  4. Land compiler-core compile tests first, including stale/inactive regressions and shared-golden render checks.
  5. Land CLI compile integration tests second, using the same corpus and goldens.
  6. Update help snapshots and doc drift guards only after the CLI surface is real.
  7. Update support-story docs and contracts in the same slice that exposes `pipeline compile` in help.
  8. Call M2 shipped only when compiler tests, CLI tests, shared goldens, help snapshots, and docs/help parity are all green together.

**Lane C Checklist**
- [ ] `stage.10_feature_spec` is declared in both the real and proof-corpus `foundation_inputs` pipelines.
- [ ] The shared corpus repo contains every stage.10 include, library input, and required artifact input.
- [ ] The shared corpus baseline contains realistic optional foundation inputs.
- [ ] No duplicate compile corpus tree exists for compiler vs CLI.
- [ ] Shared compile payload and explain goldens are committed under the foundation-inputs goldens directory.
- [ ] Missing, malformed, stale, and inactive route-basis cases are covered.
- [ ] Required-input refusal and optional-input success are both covered for `stage.10_feature_spec`.
- [ ] Help snapshots and docs/help drift guards are updated for `pipeline compile` and `--explain`.
- [ ] `docs/SUPPORTED_COMMANDS.md`, `README.md`, and `docs/START_HERE.md` no longer describe compile as deferred once shipped.
- [ ] M2 is not called shipped until the shared corpus, goldens, tests, help snapshots, and docs all agree.