[package]
name = "handbook-flow"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "handbook_flow"
path = "src/lib.rs"

[dependencies]
handbook-engine = { path = "../engine" }

[dev-dependencies]
tempfile = "3"
