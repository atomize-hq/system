Emit one bounded handoff bundle for `pipeline.foundation_inputs` -> `feature-slice-decomposer`

Usage: handbook pipeline handoff emit --id <ID> --consumer <CONSUMER>

Options:
      --id <ID>              Canonical id or unambiguous shorthand for a pipeline
      --consumer <CONSUMER>  Supported downstream consumer id
  -h, --help                 Print help

Example:
  handbook pipeline handoff emit --id pipeline.foundation_inputs --consumer feature-slice-decomposer
