[package]
name = "handbook-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "handbook"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde_json = "1"
handbook-compiler = { path = "../compiler" }

[dev-dependencies]
portable-pty = "0.8"
tempfile = "3"
