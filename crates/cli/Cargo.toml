[package]
name = "system-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "system"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
system-compiler = { path = "../compiler" }
